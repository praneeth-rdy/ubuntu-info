'sudo ln -s /path/to/origin /path/to/doop' to create a simlink of origin at doop

* Few services like mysql, apache would stop and maynot restart due to some misuse or files like logs are missing.
So, sometimes just creating new files in place would solve the issue.


Desktop Entries:
/usr/share/applications
