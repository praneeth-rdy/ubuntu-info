This is a static site generator. Generates a static compiled html page on initial request and later the other pages are rendered using react, gatsby on the browser itself.


To install gatsby globally: npm install -g gatsby-cli
